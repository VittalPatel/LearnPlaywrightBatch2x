export let testName: string = "Login Test";

function add_ts(a: number, b: number): number {
    return a + b;
}

console.log("Test Name:", testName);

let result: number = add_ts(10, 20);
console.log("Addition Result:", result);